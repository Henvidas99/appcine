import 'package:flutter/material.dart';

class TabTvScreen extends StatelessWidget {
  const TabTvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab Two Content'),
    );
  }
}
